import { DynamicDialogConfig } from "primeng/dynamicdialog";

export const workoutDialogConfig: DynamicDialogConfig = {
  width: '70vw',
  height: '75vh',
  dismissableMask: true,
  maximizable: true,
}
